[build]
builder = "nixpacks"
buildCommand = "pip3 install --break-system-packages -r backend/requirements.txt"

[deploy]
startCommand = "cd backend && python3 -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
